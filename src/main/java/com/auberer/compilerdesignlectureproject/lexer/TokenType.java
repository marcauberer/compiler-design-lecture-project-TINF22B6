package com.auberer.compilerdesignlectureproject.lexer;

public enum TokenType {
  TOK_INVALID,
  // ToDo: Insert tokens here
  TOK_STRING_LITERAL,
  TOK_INTEGER_LITERAL,
  TOK_DOUBLE_LITERAL,
  TOK_IDENTIFIER,
  TOK_KEYWORD,
  TOK_EOF,
}
